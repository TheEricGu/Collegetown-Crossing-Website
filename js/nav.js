const toggleButton = document.getElementsByClassName("toggle-button")[0], navbarLinks = document.getElementsByClassName("navbar-links")[0]; toggleButton.addEventListener("click", () => { navbarLinks.classList.toggle("active"), navbarLinks.forEach((t, a) => { t.style.animation ? t.style.animation = "" : t.style.animation = `navLinkFade 0.5s ease forwards ${a / 7 + 1.5}s` }) });